<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %5p %c{1}:%L - %m%n</pattern>
        </encoder>
    </appender>

    <appender name="STDOUT_JSON" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LogstashEncoder"/>
    </appender>

    <if condition='"${NAIS_CLUSTER_NAME}" == "prod-gcp"'>
        <then>
            <root level="INFO">
                <appender-ref ref="STDOUT_JSON"/>
            </root>
            <logger name="no.nav.paw" level="INFO"/>
            <logger name="io.ktor.server.plugins.cors" level="WARN"/>
            <logger name="org.apache.kafka" level="WARN"/>
            <logger name="io.confluent.kafka" level="WARN"/>
            <include resource="no/nav/common/audit_log/logback-naudit.xml"/>
        </then>
    </if>
    <if condition='"${NAIS_CLUSTER_NAME}" == "dev-gcp"'>
        <then>
            <logger name="no.nav" level="DEBUG"/>
            <root level="INFO">
                <appender-ref ref="STDOUT_JSON"/>
            </root>
            <logger name="no.nav.paw" level="INFO"/>
            <logger name="io.ktor.server.plugins.cors" level="WARN"/>
            <logger name="org.apache.kafka" level="WARN"/>
            <logger name="io.confluent.kafka" level="WARN"/>
            <include resource="no/nav/common/audit_log/logback-naudit.xml"/>
        </then>
    </if>
    <if condition='"${NAIS_CLUSTER_NAME}" == "docker"'>
        <then>
            <logger name="no.nav" level="DEBUG"/>
            <logger name="org.apache.kafka" level="WARN"/>
            <logger name="io.confluent.kafka" level="WARN"/>
            <root level="INFO">
                <appender-ref ref="STDOUT"/>
            </root>
        </then>
    </if>
    <if condition='"${NAIS_CLUSTER_NAME}" == "NAIS_CLUSTER_NAME_IS_UNDEFINED"'>
        <then>
            <logger name="no.nav" level="DEBUG"/>
            <logger name="no.nav.paw.logger.application" level="TRACE"/>
            <logger name="org.apache.kafka" level="WARN"/>
            <logger name="io.confluent.kafka" level="WARN"/>
            <root level="INFO">
                <appender-ref ref="STDOUT"/>
            </root>
        </then>
    </if>
</configuration>